#!/usr/bin/python3
def weight_average(my_list=[]):
    if not my_list:
        return 0
    adding = 0
    multipy = 0

    for i in my_list:
        multiply += i[0] * i[1]
        adding += i[1]
    return (multiply / adding)
